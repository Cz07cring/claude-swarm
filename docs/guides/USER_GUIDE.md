# Claude Swarm 使用指南

**版本**: v2.0
**更新时间**: 2026-01-30

---

## 🚀 快速开始

### 最简单的使用方式

```bash
# 1. 启动 3 个 agent
./swarm start --agents 3

# 2. 添加一个测试任务
./swarm add-task "创建一个 test.txt 文件，内容为 'Hello from Claude Swarm'"

# 3. 查看状态
./swarm status

# 4. 查看实时输出（可选）
tmux attach -t claude-swarm
# 退出查看：按 Ctrl+B 然后按 D

# 5. 完成后停止
./swarm stop
```

---

## 📋 完整使用流程

### 方式 1: 手动模式（传统方式）

适合：已知具体任务列表，需要精确控制

```bash
# 步骤 1: 启动 Agent 集群
./swarm start --agents 5

# 步骤 2: 添加任务
./swarm add-task "实现用户登录 API"
./swarm add-task "实现用户注册 API"
./swarm add-task "编写单元测试"
./swarm add-task "添加 API 文档"

# 步骤 3: 监控进度
./swarm status

# 步骤 4: 停止集群
./swarm stop
```

### 方式 2: AI 主脑模式（推荐 🧠）

适合：复杂功能开发，让 AI 自动拆分任务

```bash
# 一句话启动完整开发流程
./swarm orchestrate "实现一个 Todo 应用，包括添加、删除、完成功能"

# AI 会自动：
# 1. 分析需求
# 2. 拆分成 8-15 个具体任务
# 3. 识别依赖关系
# 4. 创建任务队列

# 然后启动 Agent 并行执行
./swarm start --agents 8

# 查看进度
./swarm status
```

**注意**: AI 主脑需要 Gemini API Key：

```bash
export GEMINI_API_KEY="your-api-key-here"
# 建议添加到 ~/.bashrc 或 ~/.zshrc
echo 'export GEMINI_API_KEY="your-key"' >> ~/.bashrc
```

---

## 📖 命令详解

### `swarm start` - 启动 Agent 集群

```bash
# 基础用法
./swarm start                    # 启动 3 个 agent（默认）
./swarm start --agents 5         # 启动 5 个 agent
./swarm start -n 10              # 启动 10 个 agent
./swarm start -n 5 -i 3          # 5 个 agent，监控间隔 3 秒

# 完整参数
./swarm start \
  --agents 5 \              # Agent 数量（1-100）
  --interval 3 \            # 监控间隔秒数（默认 5）
  --session my-swarm        # tmux 会话名称（默认 claude-swarm）
```

**参数说明**:
- `-n, --agents`: Agent 数量，建议 3-20 个
- `-i, --interval`: 监控间隔，建议 2-10 秒
- `-s, --session`: tmux 会话名称

**验证启动成功**:
```bash
# 检查 tmux 会话
tmux ls

# 查看 Agent 状态
./swarm status

# 检查 worktrees
git worktree list
```

---

### `swarm orchestrate` - AI 主脑分析 🧠

```bash
# 基础用法
./swarm orchestrate "需求描述"

# 示例
./swarm orchestrate "实现一个博客系统，支持文章发布、评论、标签"
./swarm orchestrate "为项目添加用户认证功能"
./swarm orchestrate "优化数据库查询性能"

# 指定 API Key（如果未设置环境变量）
./swarm orchestrate --api-key "your-key" "需求描述"

# 分析后自动启动
./swarm orchestrate --auto-start -n 10 "需求描述"
```

**AI 主脑会输出**:
```
🧠 AI主脑启动中...
📝 需求: 实现一个博客系统

🔍 AI分析需求中...

📊 AI分析结果
════════════════════════════════════════════════════════════
📌 需求概要: 博客系统，包含文章、评论、标签功能
🎯 复杂度: medium
⏱️  预计时间: 12-16h

🔧 模块拆分 (5个模块):
  1. DatabaseSchema - 数据库表设计
  2. ArticleAPI - 文章 CRUD API
  3. CommentSystem - 评论功能
  4. TagManagement - 标签管理
  5. Testing - 测试和文档

📋 任务列表 (12个任务):
  🟢 Task-1: 创建数据库表...
  🔵 Task-2: 实现 POST /api/articles...
  ...

✅ 任务队列创建完成！共 12 个任务
```

---

### `swarm add-task` - 添加任务

```bash
# 添加单个任务
./swarm add-task "修复登录页面的 bug"

# 任务描述可以很详细
./swarm add-task "实现用户注册功能：
  - 添加 POST /api/register 接口
  - 验证邮箱格式
  - 密码加密存储
  - 发送验证邮件"

# 批量添加
for module in auth user profile order; do
    ./swarm add-task "为 ${module} 模块编写单元测试"
done
```

**任务描述最佳实践**:
- ✅ 具体明确："实现 POST /api/login 接口"
- ✅ 包含验收标准："添加测试用例，覆盖率 > 80%"
- ✅ 合理粒度：30分钟 - 2小时完成
- ❌ 过于模糊："做一些优化"
- ❌ 粒度过大："完成整个项目"

---

### `swarm status` - 查看状态

```bash
./swarm status
```

**输出示例**:
```
📊 Claude Agent Swarm 状态
============================================================

✓ 会话: claude-swarm (运行中)
  窗格数量: 5

📋 任务队列: 10 个任务
  状态统计:
    待处理: 3
    进行中: 2
    已完成: 5

  最近任务:
    ✅ task-123 | 编写单元测试
      状态: completed | Agent: agent-1 | 5 分钟前
    🔄 task-124 | 实现登录 API
      状态: in_progress | Agent: agent-0 | 10 分钟前

============================================================

💡 提示:
  - 查看实时输出: tmux attach -t claude-swarm
  - 添加任务: swarm add-task "任务描述"
  - 停止集群: swarm stop
```

---

### `swarm stop` - 停止集群

```bash
./swarm stop
```

**自动清理内容**:
- ✅ tmux 会话
- ✅ Git worktrees 目录
- ✅ Agent 分支
- ✅ PID 文件
- ✅ 遗留进程

**验证清理成功**:
```bash
# 所有应该返回空或不存在
tmux ls                     # 应该没有 claude-swarm
ls -la .worktrees          # 应该不存在
git branch | grep agent    # 应该没有 agent 分支
ps aux | grep "swarm"      # 应该没有遗留进程
```

---

## 💡 使用场景

### 场景 1: 并行开发独立功能

**适合**: 多个互不依赖的功能模块

```bash
# 启动多个 Agent
./swarm start --agents 5

# 添加独立任务
./swarm add-task "实现用户登录 API"
./swarm add-task "实现用户注册 API"
./swarm add-task "实现密码重置 API"
./swarm add-task "编写 API 文档"
./swarm add-task "添加单元测试"

# 时间对比
# 串行开发: 5 × 30分钟 = 2.5小时
# 并行开发: 5 ÷ 5个agent ≈ 30分钟
# 节省时间: 80%
```

---

### 场景 2: 批量重复任务

**适合**: 相似的重复性工作

```bash
./swarm start --agents 10

# 为所有模块添加测试
modules=("auth" "user" "profile" "order" "payment" "shipping" "cart" "product" "review" "search")

for module in "${modules[@]}"; do
    ./swarm add-task "为 ${module} 模块编写单元测试"
    ./swarm add-task "为 ${module} 模块编写集成测试"
done

# 20 个测试任务，10 个 Agent 并行，预计 1 小时完成
```

---

### 场景 3: AI 驱动的完整功能开发 🧠

**适合**: 复杂功能，需求明确但实现细节需要规划

```bash
# 一句话启动整个开发流程
./swarm orchestrate "开发一个实时聊天系统，支持：
  - 文本消息
  - 图片发送
  - 在线状态显示
  - 消息历史记录
  - 未读消息提醒"

# AI 自动生成 15-20 个任务：
# Module 1: WebSocket 连接管理
# Module 2: 消息存储和检索
# Module 3: 文件上传处理
# Module 4: 在线状态同步
# Module 5: 前端组件
# Module 6: 测试和文档

# 启动 10 个 Agent 并行开发
./swarm start --agents 10

# 预计 4-6 小时完成（传统开发需要 2-3 天）
```

---

### 场景 4: Bug 修复和优化

**适合**: 多个独立的 bug 或优化点

```bash
./swarm start --agents 3

# 添加 bug 修复任务
./swarm add-task "修复登录页面在移动端的布局问题"
./swarm add-task "解决用户头像上传后不显示的 bug"
./swarm add-task "优化首页加载速度（目标 < 2秒）"
```

---

### 场景 5: 文档和测试补充

**适合**: 为现有代码添加文档和测试

```bash
./swarm start --agents 8

# 添加文档任务
./swarm add-task "为 API 接口生成 OpenAPI 文档"
./swarm add-task "编写用户使用手册"
./swarm add-task "更新 README 安装说明"

# 添加测试任务
./swarm add-task "为核心业务逻辑添加单元测试"
./swarm add-task "编写 E2E 测试用例"
./swarm add-task "添加性能测试"
```

---

## 🔍 监控和调试

### 查看实时输出

**方法 1: 附加到 tmux 会话**

```bash
# 进入 tmux 会话
tmux attach -t claude-swarm

# tmux 快捷键
Ctrl+B + 方向键    # 切换窗格
Ctrl+B + D         # 退出（不停止会话）
Ctrl+B + [         # 进入滚动模式（可以向上翻页）
  然后按 q 退出滚动模式
```

**方法 2: 捕获特定 Agent 输出**

```bash
# 捕获 agent-0 的最近 50 行输出
tmux capture-pane -t claude-swarm:0.0 -p | tail -50

# 捕获 agent-1 的输出
tmux capture-pane -t claude-swarm:0.1 -p | tail -50
```

---

### 检查任务队列

```bash
# 查看任务队列文件
cat ~/.claude-swarm/tasks.json

# 使用 jq 美化输出
cat ~/.claude-swarm/tasks.json | jq

# 统计各状态任务数量
jq '.tasks | group_by(.status) | map({status: .[0].status, count: length})' \
   ~/.claude-swarm/tasks.json

# 查看进行中的任务
jq '.tasks[] | select(.status=="in_progress")' ~/.claude-swarm/tasks.json
```

---

### 查看日志

```bash
# 如果 swarm 在后台运行，查看日志
tail -f /tmp/swarm-*.log

# 搜索关键信息
grep "合并" /tmp/swarm-*.log
grep "ERROR" /tmp/swarm-*.log
```

---

## ⚠️ 常见问题和解决方案

### Q1: 启动失败，提示 "已在运行中"

**原因**: 上次的 swarm 进程未正常退出

**解决**:
```bash
# 方法 1: 使用 stop 命令
./swarm stop

# 方法 2: 手动清理
pkill -9 -f "swarm start"
rm ~/.claude-swarm/*.pid
rm -rf .worktrees
```

---

### Q2: Agent 卡住不工作

**现象**: 任务状态一直是 "in_progress"，但没有进展

**排查**:
```bash
# 1. 查看状态
./swarm status

# 2. 进入 tmux 查看 Agent 输出
tmux attach -t claude-swarm

# 3. 检查是否在等待用户确认
# 如果看到 "(y/N)" 提示，Agent 在等待输入
```

**解决**:
```bash
# 如果确实卡住，重启 Agent
./swarm stop
./swarm start --agents 3
```

---

### Q3: 任务添加后没有被执行

**排查**:
```bash
# 1. 确认 Agent 在运行
./swarm status

# 2. 检查任务队列
cat ~/.claude-swarm/tasks.json | jq '.tasks'

# 3. 查看任务状态
./swarm status
```

**可能原因**:
- Agent 未启动
- 所有 Agent 都在忙
- 任务队列文件损坏

**解决**:
```bash
# 如果队列损坏，重置队列
mv ~/.claude-swarm/tasks.json ~/.claude-swarm/tasks.json.backup
echo '{"tasks":[]}' > ~/.claude-swarm/tasks.json
```

---

### Q4: Git 合并冲突

**现象**: 日志中出现 "合并冲突" 提示

**说明**: 系统会自动尝试解决冲突，如果无法自动解决，任务会标记为失败

**手动处理**:
```bash
# 1. 查看冲突文件
git status

# 2. 手动解决冲突
# 编辑冲突文件，保留正确的代码

# 3. 提交解决
git add .
git commit -m "解决合并冲突"
```

---

### Q5: worktrees 清理失败

**现象**: stop 后 .worktrees 目录仍存在

**解决**:
```bash
# 手动清理 worktrees
git worktree list
git worktree remove .worktrees/agent-0 --force
git worktree remove .worktrees/agent-1 --force

# 删除目录
rm -rf .worktrees

# 清理分支
git branch -D agent-0-branch agent-1-branch
```

---

## 🎯 最佳实践

### 1. Agent 数量选择

| 任务数量 | 推荐 Agent 数 | 说明 |
|---------|-------------|------|
| 1-3 个  | 2-3         | 小任务，避免资源浪费 |
| 4-10 个 | 5-8         | 中等规模 |
| 10-20 个 | 10-15      | 大型项目 |
| 20+ 个  | 15-20       | 超大项目（注意资源） |

**注意**: Agent 过多会消耗大量 CPU 和内存，建议根据机器配置调整。

---

### 2. 任务拆分原则

**✅ 好的任务拆分**:
```bash
./swarm add-task "实现 POST /api/users 接口，接收 email 和 password，返回 user_id"
./swarm add-task "添加用户注册的输入验证，邮箱格式检查和密码强度检查"
./swarm add-task "为用户注册接口编写单元测试，覆盖成功和失败场景"
```

**❌ 不好的任务拆分**:
```bash
./swarm add-task "做一些优化"  # 太模糊
./swarm add-task "完成用户系统"  # 粒度太大
./swarm add-task "修改第 123 行代码"  # 太细碎
```

**原则**:
- 每个任务 30 分钟 - 2 小时
- 任务之间尽量独立
- 描述清晰，包含验收标准

---

### 3. 使用 AI 主脑的时机

**✅ 适合使用 AI 主脑**:
- 新功能开发（从 0 到 1）
- 复杂系统重构
- 需求明确但实现细节未定
- 模块化的大型项目

**❌ 不适合 AI 主脑**:
- 单文件小修改
- 紧急 bug 修复
- 高度耦合的遗留代码
- 需要大量上下文的调试

---

### 4. 监控频率建议

```bash
# 开发初期：频繁监控
watch -n 10 './swarm status'  # 每 10 秒刷新

# 稳定运行后：偶尔查看
./swarm status  # 手动查看

# 长时间运行：定时检查
while true; do
    ./swarm status
    sleep 300  # 每 5 分钟
done
```

---

### 5. 资源管理

**磁盘空间**:
- 每个 worktree 约占 50-200MB
- 建议至少 5GB 可用空间

**内存**:
- 每个 Agent 约占 100-500MB
- 建议：Agent 数量 × 500MB < 可用内存

**CPU**:
- 每个 Agent 约占 10-30% CPU
- 建议：Agent 数量 < CPU 核心数 × 2

---

## 📚 进阶使用

### 自定义监控间隔

```bash
# 高频监控（快速响应）
./swarm start -n 5 -i 2  # 2 秒检查一次

# 低频监控（节省资源）
./swarm start -n 10 -i 10  # 10 秒检查一次
```

---

### 配置文件使用

创建 `config.yaml`:

```yaml
# Agent 配置
default_agents: 5
monitor_interval: 5

# Gemini API
gemini_api_key: "your-api-key-here"

# 路径配置
task_queue_path: "~/.claude-swarm/tasks.json"
agent_state_path: "~/.claude-swarm/agents.json"
```

---

### 集成到 CI/CD

```yaml
# .github/workflows/parallel-tests.yml
name: Parallel Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Claude Swarm
        run: |
          go build -o swarm ./cmd/swarm

      - name: Run Parallel Tests
        run: |
          ./swarm start --agents 10
          ./swarm add-task "运行单元测试"
          ./swarm add-task "运行集成测试"
          ./swarm add-task "运行 E2E 测试"

          # 等待完成
          while ./swarm status | grep "进行中"; do
            sleep 10
          done

          ./swarm stop
```

---

## 🆘 获取帮助

### 内置帮助

```bash
# 查看所有命令
./swarm --help

# 查看特定命令帮助
./swarm start --help
./swarm orchestrate --help
```

### 文档

- **完整文档**: [README.md](README.md)
- **问题报告**: [ISSUES_DETECTED.md](ISSUES_DETECTED.md)
- **修复记录**: [FIXES.md](FIXES.md)
- **配置指南**: [CONFIG_GUIDE.md](CONFIG_GUIDE.md)

### 问题反馈

- GitHub Issues: [提交问题](https://github.com/Cz07cring/claude-swarm/issues)

---

## 🎓 总结

Claude Swarm 让你能够：

1. **并行开发** - 10 个任务同时进行，节省 80% 时间
2. **AI 辅助** - 一句话描述需求，AI 自动拆分任务
3. **自动化** - 自动任务调度、冲突解决、资源清理
4. **可靠性** - 完善的错误处理和恢复机制

**开始你的第一个 Swarm 任务吧！** 🚀

```bash
./swarm start --agents 3
./swarm add-task "你的第一个任务"
```

---

*文档版本: 2.0*
*最后更新: 2026-01-30*
